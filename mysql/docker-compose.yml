version: '3.7'
services:
  db:
    image: mysql:5.7
    container_name: banco_mysql
    volumes:
      - ./:/home

    ports:
      - '3306:3306'
    environment:
      MYSQL_DATABASE: 'empresa'
      MYSQL_USER: 'empresa'
      MYSQL_PASSWORD: 'empresa'
      MYSQL_ROOT_PASSWORD: 'root'
